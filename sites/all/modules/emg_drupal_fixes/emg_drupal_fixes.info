name = EMG Drupal Fixes
description = "This fixes wierd anomalies and bugs (CSS Mostly) that have not been fixed, patched, repostorized by the drupal community and would preferable not to have to be un expectedly fixed on every site going forward."
package = Other
core = 7.x

dependencies[] = hook_post_action
dependencies[] = browserclass
dependencies[] = modernizr

stylesheets[all][] = emg_drupal_fixes.css

scripts[] = js/emg_drupal_fixes.js